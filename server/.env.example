DATABASE_URL="postgresql://careslot:careslot@localhost:5433/careslot?schema=public"
PORT=3000
JWT_SECRET=change-me
JWT_EXPIRES_IN_SECONDS=3600
NODE_ENV=development

# Comma-separated list of browser origins allowed to call the API directly
# (cross-origin, with cookies). Not needed when the SPA is served by the API
# or proxied through the Vite dev server, but harmless to keep.
CORS_ORIGINS=http://localhost:5173
