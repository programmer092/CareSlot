{
  "name": "careslot",
  "version": "1.0.0",
  "private": true,
  "description": "CareSlot monorepo (NestJS server + Vite/React client)",
  "license": "ISC",
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"pnpm --filter server start:dev\" \"pnpm --filter client dev\"",
    "build": "pnpm --filter client build && mkdir -p server/public && cp -R client/dist/. server/public/ && pnpm --filter server build",
    "test": "pnpm --filter server test:all",
    "test:unit": "pnpm --filter server test",
    "test:e2e": "pnpm --filter server test:e2e",
    "lint": "pnpm -r run lint",
    "seed": "pnpm --filter server prisma:seed",
    "clean": "rimraf server/dist client/dist server/public"
  },
  "devDependencies": {
    "concurrently": "^8.0.0",
    "rimraf": "^3.0.2"
  }
}